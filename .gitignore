# Credentials. .env.* also covers timestamped backups, which hold live values.
.env
.env.*
!.env.example

.venv/
__pycache__/
*.pyc

# Browser-automation artifacts (page snapshots, console logs).
.playwright-mcp/

# uv.lock is deliberately NOT ignored — it pins the TAC git dependency to an
# exact commit, and the deploy ships only committed files.
